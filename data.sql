CREATE DATABASE todoapp;

CREATE TABLE todo(
    id VARCHAR(255) PRIMARY KEY,
    email VARCHAR(255),
    title VARCHAR(255),
    progress VARCHAR(30),
    data VARCHAR(255)
);

CREATE TABLE users (
email VARCHAR(255) PRIMARY KEY,
password VARCHAR
);

